# IMDb with Serenity and Cucumber

## Executing the tests
To run the tests, use `mvn clean verify` from the command line.


### Tests are running in parallel and the configuration is updated in `serenity.properties`
